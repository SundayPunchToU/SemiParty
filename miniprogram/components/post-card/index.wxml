<view class="post-card" bindtap="handleTap">
  <view class="post-head">
    <view class="avatar post-avatar">
      {{item.author.avatarText}}
    </view>
    <view class="post-author">
      <view class="post-name">{{item.author.name}}</view>
      <view class="post-role">{{item.author.title}}</view>
    </view>
  </view>

  <view class="post-content">{{item.content}}</view>
  <text wx:if="{{topicText}}" class="tag">{{topicText}}</text>

  <view class="post-actions">
    <view class="post-action {{liked ? 'is-active' : ''}}" catchtap="handleLike">赞 {{likeCount}}</view>
    <view class="post-action" catchtap="handleComment">评 {{item.comments || item.commentCount || 0}}</view>
    <view class="post-action" catchtap="handleShare">分享</view>
  </view>
  <view class="post-time">{{relativeTime}}</view>
</view>
